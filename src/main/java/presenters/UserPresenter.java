package presenters;

import entities.User;

public class UserPresenter implements UserPresenterInt  {

    /**
     * the presenter method that returns data of a User class to the UI,
     * and transform into UI elements
     * @param user
     */
    public void show(User user) {

    }
}